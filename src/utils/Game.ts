export type Game = {
    id: number,
    name: string,
    released: string,
    background_image: string,
    metacritic: number,
    price: number
}

export type Price = {
    cheapest: string;
    external: string;
}